# Pre-reset recovery

`pre_reset_6a228f7.bundle` is a verified Git bundle containing the complete pre-reset history at commit `6a228f7`. It was created before removing obsolete gameplay files. A separate checkout can be made with:

```sh
git clone recovery/pre_reset_6a228f7.bundle ../slop-before-reset
```

The current working tree's `HEAD` is also still that pre-reset commit until these new changes are committed.
